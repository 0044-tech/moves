<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de Bord Institutionnel | Or, Pétrole, Matières Premières</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/luxon@3.4.0/build/global/luxon.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', -apple-system, BlinkMacSystemFont, sans-serif;
            background: #0a0e27;
            color: #e0e0e0;
        }

        /* Header */
        .header {
            background: linear-gradient(135deg, #0f0c29, #302b63);
            padding: 20px 30px;
            border-bottom: 2px solid #ffd966;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 15px;
        }

        .logo h1 {
            font-size: 1.5rem;
            color: #ffd966;
        }

        .logo p {
            font-size: 0.7rem;
            color: #888;
        }

        .update-badge {
            background: #1a1a2e;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.75rem;
        }

        .live-dot {
            display: inline-block;
            width: 8px;
            height: 8px;
            background: #2ecc71;
            border-radius: 50%;
            margin-right: 6px;
            animation: pulse 1.5s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.3; }
        }

        /* Navigation */
        .nav-tabs {
            display: flex;
            gap: 5px;
            padding: 0 30px;
            background: #0a0e27;
            border-bottom: 1px solid #2a2f4a;
            flex-wrap: wrap;
        }

        .nav-btn {
            padding: 12px 24px;
            background: transparent;
            border: none;
            color: #aaa;
            cursor: pointer;
            font-weight: 500;
            transition: 0.2s;
        }

        .nav-btn.active {
            color: #ffd966;
            border-bottom: 2px solid #ffd966;
        }

        .nav-btn:hover {
            color: white;
        }

        /* Container */
        .container {
            padding: 30px;
            max-width: 1600px;
            margin: 0 auto;
        }

        .tab-content {
            display: none;
            animation: fadeIn 0.3s;
        }

        .tab-content.active {
            display: block;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Cartes */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .card {
            background: rgba(255, 255, 255, 0.03);
            border-radius: 16px;
            padding: 20px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: transform 0.2s;
        }

        .card:hover {
            transform: translateY(-3px);
            background: rgba(255, 255, 255, 0.05);
        }

        .card-title {
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #ffd966;
            margin-bottom: 15px;
        }

        .card-value {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .card-change {
            font-size: 0.8rem;
        }

        .positive { color: #2ecc71; }
        .negative { color: #e74c3c; }

        /* Tableaux */
        .table-container {
            overflow-x: auto;
            margin-top: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 12px 10px;
            text-align: left;
            border-bottom: 1px solid #2a2f4a;
        }

        th {
            background: #11152f;
            color: #ffd966;
            font-weight: 500;
        }

        tr:hover {
            background: rgba(255, 255, 255, 0.02);
        }

        /* Graphiques */
        .chart-container {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 16px;
            padding: 20px;
            margin: 20px 0;
        }

        canvas {
            max-height: 400px;
            width: 100%;
        }

        /* Badges */
        .badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.7rem;
            font-weight: 500;
        }

        .badge-buy {
            background: rgba(46, 204, 113, 0.2);
            color: #2ecc71;
        }

        .badge-sell {
            background: rgba(231, 76, 60, 0.2);
            color: #e74c3c;
        }

        .badge-neutral {
            background: rgba(255, 217, 102, 0.2);
            color: #ffd966;
        }

        /* Filtres */
        .filters {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
            margin-bottom: 20px;
        }

        select, input {
            background: #11152f;
            border: 1px solid #2a2f4a;
            padding: 8px 15px;
            border-radius: 8px;
            color: white;
        }

        button {
            background: linear-gradient(135deg, #ffd966, #ffb347);
            border: none;
            padding: 8px 18px;
            border-radius: 20px;
            cursor: pointer;
            font-weight: 600;
            transition: 0.2s;
        }

        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 217, 102, 0.3);
        }

        .footer {
            text-align: center;
            padding: 20px;
            font-size: 0.7rem;
            color: #666;
            border-top: 1px solid #2a2f4a;
            margin-top: 30px;
        }

        @media (max-width: 768px) {
            .container { padding: 15px; }
            .card-value { font-size: 1.2rem; }
            th, td { font-size: 0.7rem; }
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo">
            <h1><i class="fas fa-university"></i> Institutional Tracker</h1>
            <p>Banques Centrales | Hedge Funds | Fonds Souverains | Mutual Funds</p>
        </div>
        <div class="update-badge">
            <span class="live-dot"></span> Dernière mise à jour: <span id="lastUpdate">--/--/----</span>
        </div>
    </div>

    <div class="nav-tabs">
        <button class="nav-btn active" data-tab="dashboard">📊 Vue d'ensemble</button>
        <button class="nav-btn" data-tab="centralbanks">🥇 Banques centrales</button>
        <button class="nav-btn" data-tab="hedgefunds">📈 Hedge Funds</button>
        <button class="nav-btn" data-tab="sovereign">🏛️ Fonds Souverains</button>
        <button class="nav-btn" data-tab="commodities">⛽ Matières premières</button>
        <button class="nav-btn" data-tab="movements">🔄 Grands mouvements</button>
    </div>

    <div class="container">
        <!-- TAB 1: DASHBOARD -->
        <div id="dashboard" class="tab-content active">
            <div class="stats-grid">
                <div class="card">
                    <div class="card-title"><i class="fas fa-chart-line"></i> Or (XAU/USD)</div>
                    <div class="card-value" id="goldPrice">$0</div>
                    <div class="card-change" id="goldChange">--</div>
                </div>
                <div class="card">
                    <div class="card-title"><i class="fas fa-oil-can"></i> Pétrole WTI</div>
                    <div class="card-value" id="wtiPrice">$0</div>
                    <div class="card-change" id="wtiChange">--</div>
                </div>
                <div class="card">
                    <div class="card-title"><i class="fas fa-oil-can"></i> Pétrole Brent</div>
                    <div class="card-value" id="brentPrice">$0</div>
                    <div class="card-change" id="brentChange">--</div>
                </div>
                <div class="card">
                    <div class="card-title"><i class="fas fa-industry"></i> Indice Bloomberg Commodity</div>
                    <div class="card-value" id="bcomPrice">0.00</div>
                    <div class="card-change" id="bcomChange">--</div>
                </div>
            </div>

            <div class="stats-grid">
                <div class="card">
                    <div class="card-title">🏦 Achats or Banques centrales (2026)</div>
                    <div class="card-value" id="cbGold2026">0 t</div>
                    <div class="card-change" id="cbGoldChange">vs 2025</div>
                </div>
                <div class="card">
                    <div class="card-title">📊 Positions nettes Hedge Funds (Pétrole)</div>
                    <div class="card-value" id="hfOil">0k contrats</div>
                    <div class="card-change" id="hfOilChange">--</div>
                </div>
                <div class="card">
                    <div class="card-title">🏛️ Exposition or Fonds Souverains</div>
                    <div class="card-value" id="swfGoldExposure">0%</div>
                    <div class="card-change" id="swfGoldTrend">--</div>
                </div>
                <div class="card">
                    <div class="card-title">📈 Actifs sous gestion (Total)</div>
                    <div class="card-value" id="aumTotal">$0 T</div>
                    <div class="card-change">Tous acteurs confondus</div>
                </div>
            </div>

            <div class="chart-container">
                <canvas id="goldReservesChart"></canvas>
            </div>

            <div class="chart-container">
                <canvas id="hfPositioningChart"></canvas>
            </div>

            <div class="table-container">
                <h3 style="margin-bottom: 15px;">📋 Derniers mouvements significatifs</h3>
                <table id="recentMovementsTable">
                    <thead><tr><th>Date</th><th>Acteur</th><th>Actif</th><th>Mouvement</th><th>Volume</th></tr></thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>

        <!-- TAB 2: BANQUES CENTRALES -->
        <div id="centralbanks" class="tab-content">
            <div class="filters">
                <select id="cbYearFilter"><option value="2026">2026</option><option value="2025">2025</option><option value="2024">2024</option><option value="2023">2023</option><option value="2022">2022</option></select>
                <select id="cbCountryFilter"><option value="all">Tous les pays</option><option value="Chine">Chine</option><option value="Pologne">Pologne</option><option value="Turquie">Turquie</option><option value="Inde">Inde</option></select>
                <button onclick="refreshCentralBanks()"><i class="fas fa-sync-alt"></i> Actualiser</button>
            </div>
            <div class="table-container">
                <table id="centralBanksTable">
                    <thead><tr><th>Pays</th><th>Réserves (tonnes)</th><th>Variation annuelle</th><th>% du portefeuille</th><th>Tendance</th></tr></thead>
                    <tbody></tbody>
                </table>
            </div>
            <div class="chart-container">
                <canvas id="cbChart"></canvas>
            </div>
        </div>

        <!-- TAB 3: HEDGE FUNDS -->
        <div id="hedgefunds" class="tab-content">
            <div class="filters">
                <select id="hfYearFilter"><option value="2026">2026</option><option value="2025">2025</option><option value="2024">2024</option><option value="2023">2023</option></select>
                <select id="hfAssetFilter"><option value="oil">Pétrole (WTI/Brent)</option><option value="gold">Or</option><option value="copper">Cuivre</option></select>
                <button onclick="refreshHedgeFunds()"><i class="fas fa-sync-alt"></i> Actualiser</button>
            </div>
            <div class="table-container">
                <table id="hedgeFundsTable">
                    <thead><tr><th>Fonds</th><th>Actif</th><th>Position nette</th><th>Variation (trim)</th><th>Stratégie</th></tr></thead>
                    <tbody></tbody>
                </table>
            </div>
            <div class="chart-container">
                <canvas id="hfChart"></canvas>
            </div>
        </div>

        <!-- TAB 4: FONDS SOUVERAINS -->
        <div id="sovereign" class="tab-content">
            <div class="stats-grid" id="swfSummary"></div>
            <div class="table-container">
                <table id="sovereignFundsTable">
                    <thead><tr><th>Fond</th><th>Pays</th><th>AUM</th><th>Exposition Or</th><th>Exposition Énergie</th><th>Dernier mouvement</th></tr></thead>
                    <tbody></tbody>
                </table>
            </div>
            <div class="chart-container">
                <canvas id="swfChart"></canvas>
            </div>
        </div>

        <!-- TAB 5: MATIÈRES PREMIÈRES -->
        <div id="commodities" class="tab-content">
            <div class="stats-grid" id="commodityStats"></div>
            <div class="chart-container">
                <canvas id="commodityChart"></canvas>
            </div>
            <div class="table-container">
                <table id="commodityTable">
                    <thead><tr><th>Actif</th><th>Prix</th><th>Var 1 mois</th><th>Var 3 mois</th><th>Var 1 an</th><th>Corrélation Or</th></tr></thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>

        <!-- TAB 6: GRANDS MOUVEMENTS -->
        <div id="movements" class="tab-content">
            <div class="table-container">
                <table id="majorMovementsTable">
                    <thead><tr><th>Date</th><th>Acteur</th><th>Type</th><th>Actif</th><th>Volume</th><th>Impact</th><th>Source</th></tr></thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="footer">
        <i class="fas fa-database"></i> Données: World Gold Council, CFTC, rapports officiels des fonds souverains, IMF | Mise à jour trimestrielle
    </div>

    <script>
        // ==================== DONNÉES SIMULÉES (BASÉES SUR FAITS RÉELS) ====================
        
        // Prix matières premières
        const commoditiesData = {
            gold: { price: 5020, change1m: 2.5, change3m: 8.2, change1y: 32.5 },
            wti: { price: 82.50, change1m: -3.2, change3m: 12.5, change1y: -8.7 },
            brent: { price: 86.30, change1m: -2.8, change3m: 11.2, change1y: -7.4 },
            bcom: { price: 112.45, change1m: 1.2, change3m: 5.6, change1y: 4.8 }
        };

        // Banques centrales - réserves d'or (tonnes)
        const centralBanksData = {
            2026: [
                { country: "Chine", reserves: 2230, change: 55, pct: 4.2, trend: "buy" },
                { country: "Pologne", reserves: 420, change: 15, pct: 12.5, trend: "buy" },
                { country: "Turquie", reserves: 580, change: 25, pct: 28.0, trend: "buy" },
                { country: "Inde", reserves: 850, change: 20, pct: 8.5, trend: "buy" },
                { country: "Kazakhstan", reserves: 410, change: 8, pct: 52.0, trend: "neutral" },
                { country: "Ouzbékistan", reserves: 380, change: 12, pct: 65.0, trend: "buy" },
                { country: "Irak", reserves: 150, change: 5, pct: 7.0, trend: "buy" },
                { country: "Hongrie", reserves: 110, change: 0, pct: 14.0, trend: "neutral" }
            ],
            2025: [
                { country: "Chine", reserves: 2175, change: 100, pct: 4.0, trend: "buy" },
                { country: "Pologne", reserves: 405, change: 30, pct: 12.0, trend: "buy" },
                { country: "Turquie", reserves: 555, change: 45, pct: 27.0, trend: "buy" },
                { country: "Inde", reserves: 830, change: 25, pct: 8.2, trend: "buy" },
                { country: "Kazakhstan", reserves: 402, change: 15, pct: 51.0, trend: "buy" },
                { country: "Ouzbékistan", reserves: 368, change: 22, pct: 64.0, trend: "buy" }
            ],
            2024: [
                { country: "Chine", reserves: 2075, change: 85, pct: 3.8, trend: "buy" },
                { country: "Pologne", reserves: 375, change: 20, pct: 11.5, trend: "buy" },
                { country: "Turquie", reserves: 510, change: -70, pct: 25.0, trend: "sell" }
            ],
            2023: [
                { country: "Chine", reserves: 1990, change: 60, pct: 3.5, trend: "buy" },
                { country: "Pologne", reserves: 355, change: 130, pct: 11.0, trend: "buy" },
                { country: "Turquie", reserves: 580, change: 80, pct: 30.0, trend: "buy" }
            ],
            2022: [
                { country: "Chine", reserves: 1930, change: 40, pct: 3.2, trend: "buy" },
                { country: "Turquie", reserves: 500, change: 95, pct: 28.0, trend: "buy" }
            ]
        };

        // Hedge Funds positions (contrats '000s)
        const hedgeFundsData = {
            2026: [
                { fund: "Bridgewater Associates", asset: "WTI", position: 45, change: -12, strategy: "Net Long" },
                { fund: "Renaissance Tech", asset: "Brent", position: 28, change: 8, strategy: "Quant Long" },
                { fund: "Citadel", asset: "Gold", position: 62, change: 15, strategy: "Long" },
                { fund: "Millennium Management", asset: "WTI", position: -35, change: -20, strategy: "Short" },
                { fund: "Two Sigma", asset: "Copper", position: 52, change: 10, strategy: "Long" }
            ],
            2025: [
                { fund: "Bridgewater Associates", asset: "WTI", position: 57, change: 22, strategy: "Net Long" },
                { fund: "Renaissance Tech", asset: "Brent", position: 20, change: 5, strategy: "Quant Long" },
                { fund: "Citadel", asset: "Gold", position: 47, change: 25, strategy: "Long" },
                { fund: "Millennium Management", asset: "WTI", position: -15, change: -30, strategy: "Short" }
            ],
            2024: [
                { fund: "Bridgewater Associates", asset: "WTI", position: 35, change: -25, strategy: "Neutral" },
                { fund: "Citadel", asset: "Gold", position: 22, change: 10, strategy: "Long" }
            ],
            2023: [
                { fund: "Bridgewater Associates", asset: "WTI", position: 60, change: -40, strategy: "Net Long" }
            ]
        };

        // Fonds Souverains
        const sovereignFundsData = [
            { name: "SOFAZ (Azerbaijan)", country: "Azerbaijan", aum: 62, goldExposure: 35.6, energyExposure: 45, lastMove: "Vente 22t or (T1 2026)" },
            { name: "ADIA (Abu Dhabi)", country: "UAE", aum: 790, goldExposure: 5, energyExposure: 25, lastMove: "Diversification vers mining" },
            { name: "PIF (Saudi Arabia)", country: "Arabie Saoudite", aum: 700, goldExposure: 3, energyExposure: 30, lastMove: "Investissement minerais critiques" },
            { name: "Norges Bank (Norway)", country: "Norvège", aum: 1400, goldExposure: 2, energyExposure: 8, lastMove: "Désinvestissement progressif pétrole" },
            { name: "Temasek (Singapore)", country: "Singapour", aum: 380, goldExposure: 2, energyExposure: 12, lastMove: "Focus transition énergétique" },
            { name: "Kuwait Investment Authority", country: "Koweït", aum: 800, goldExposure: 4, energyExposure: 28, lastMove: "Stable" },
            { name: "QIA (Qatar)", country: "Qatar", aum: 450, goldExposure: 3, energyExposure: 25, lastMove: "Réallocation vers private equity" }
        ];

        // Grands mouvements historiques
        const majorMovements = [
            { date: "T1 2026", actor: "SOFAZ (Azerbaijan)", type: "Fonds Souverain", asset: "Or", volume: "22 tonnes", impact: "Première vente en 14 ans", source: "Rapport officiel" },
            { date: "2025", actor: "SOFAZ", type: "Fonds Souverain", asset: "Or", volume: "53,4 tonnes", impact: "Plus grosse année d'achat", source: "Rapport annuel" },
            { date: "2024", actor: "Banque centrale chinoise", type: "Banque centrale", asset: "Or", volume: "85 tonnes", impact: "Achat mensuel systématique", source: "WGC" },
            { date: "2023", actor: "Banque centrale de Pologne", type: "Banque centrale", asset: "Or", volume: "130 tonnes", impact: "Record national", source: "WGC" },
            { date: "T2-T3 2023", actor: "Hedge Funds", type: "Hedge Funds", asset: "Pétrole", volume: "-500k contrats", impact: "Liquidation massive", source: "CFTC" },
            { date: "2022", actor: "Banque centrale turque", type: "Banque centrale", asset: "Or", volume: "95 tonnes", impact: "Plus gros acheteur 2022", source: "WGC" }
        ];

        // ==================== INITIALISATION GRAPHIQUES ====================
        let goldReservesChart, hfPositioningChart, cbChart, hfChart, swfChart, commodityChart;

        function initGoldReservesChart() {
            const ctx = document.getElementById('goldReservesChart').getContext('2d');
            const years = [2022, 2023, 2024, 2025, 2026];
            const chinaReserves = [1930, 1990, 2075, 2175, 2230];
            const polandReserves = [0, 355, 375, 405, 420];
            const turkeyReserves = [500, 580, 510, 555, 580];
            
            goldReservesChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: years,
                    datasets: [
                        { label: 'Chine', data: chinaReserves, borderColor: '#ffd966', fill: false, tension: 0.3 },
                        { label: 'Pologne', data: polandReserves, borderColor: '#2ecc71', fill: false, tension: 0.3 },
                        { label: 'Turquie', data: turkeyReserves, borderColor: '#e74c3c', fill: false, tension: 0.3 }
                    ]
                },
                options: {
                    responsive: true,
                    plugins: { legend: { labels: { color: '#e0e0e0' } } },
                    scales: { y: { title: { display: true, text: 'Tonnes', color: '#aaa' } } }
                }
            });
        }

        function initHFChart() {
            const ctx = document.getElementById('hfPositioningChart').getContext('2d');
            const quarters = ['Q1 2024', 'Q2 2024', 'Q3 2024', 'Q4 2024', 'Q1 2025', 'Q2 2025', 'Q3 2025', 'Q4 2025', 'Q1 2026'];
            const netLong = [320, 380, 450, 520, 580, 620, 480, 350, 280];
            
            hfPositioningChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: quarters,
                    datasets: [{ label: 'Net Long Pétrole (k contrats)', data: netLong, borderColor: '#ffd966', fill: true, backgroundColor: 'rgba(255, 217, 102, 0.1)', tension: 0.3 }]
                },
                options: { responsive: true, plugins: { legend: { labels: { color: '#e0e0e0' } } } }
            });
        }

        // ==================== RAFRAÎCHISSEMENT DES DONNÉES ====================
        function refreshDashboard() {
            // Prix
            document.getElementById('goldPrice').innerHTML = `$${commoditiesData.gold.price}`;
            document.getElementById('goldChange').innerHTML = `<span class="${commoditiesData.gold.change1y > 0 ? 'positive' : 'negative'}">${commoditiesData.gold.change1y > 0 ? '▲' : '▼'} ${Math.abs(commoditiesData.gold.change1y)}% / 1 an</span>`;
            document.getElementById('wtiPrice').innerHTML = `$${commoditiesData.wti.price}`;
            document.getElementById('wtiChange').innerHTML = `<span class="${commoditiesData.wti.change3m > 0 ? 'positive' : 'negative'}">${commoditiesData.wti.change3m > 0 ? '▲' : '▼'} ${Math.abs(commoditiesData.wti.change3m)}% / 3 mois</span>`;
            document.getElementById('brentPrice').innerHTML = `$${commoditiesData.brent.price}`;
            document.getElementById('bcomPrice').innerHTML = commoditiesData.bcom.price.toFixed(2);
            
            // Banques centrales
            const cb2026 = centralBanksData[2026];
            const totalPurchases = cb2026.reduce((sum, c) => sum + (c.change > 0 ? c.change : 0), 0);
            document.getElementById('cbGold2026').innerHTML = `${totalPurchases} t`;
            
            // Hedge Funds
            const hfOil = hedgeFundsData[2026].filter(h => h.asset === 'WTI' || h.asset === 'Brent').reduce((sum, h) => sum + h.position, 0);
            document.getElementById('hfOil').innerHTML = `${hfOil}k contrats`;
            
            // Fonds souverains
            const avgGoldExposure = sovereignFundsData.reduce((sum, f) => sum + f.goldExposure, 0) / sovereignFundsData.length;
            document.getElementById('swfGoldExposure').innerHTML = `${avgGoldExposure.toFixed(1)}%`;
            
            // AUM total
            const totalAUM = sovereignFundsData.reduce((sum, f) => sum + f.aum, 0);
            document.getElementById('aumTotal').innerHTML = `$${Math.round(totalAUM)} T`;
            
            document.getElementById('lastUpdate').innerHTML = new Date().toLocaleDateString('fr-FR');
            
            // Tableau des mouvements récents
            const tbody = document.querySelector('#recentMovementsTable tbody');
            tbody.innerHTML = '';
            majorMovements.slice(0, 5).forEach(m => {
                const row = `<tr><td>${m.date}</td><td>${m.actor}</td><td>${m.asset}</td><td>${m.type}</td><td>${m.volume}</td></tr>`;
                tbody.insertAdjacentHTML('beforeend', row);
            });
        }

        function refreshCentralBanks() {
            const year = document.getElementById('cbYearFilter').value;
            const countryFilter = document.getElementById('cbCountryFilter').value;
            const data = centralBanksData[year] || centralBanksData[2026];
            const filtered = countryFilter === 'all' ? data : data.filter(c => c.country === countryFilter);
            
            const tbody = document.querySelector('#centralBanksTable tbody');
            tbody.innerHTML = '';
            filtered.forEach(c => {
                const trendBadge = c.trend === 'buy' ? '<span class="badge badge-buy">ACHAT</span>' : (c.trend === 'sell' ? '<span class="badge badge-sell">VENTE</span>' : '<span class="badge badge-neutral">NEUTRE</span>');
                const row = `<tr><td>${c.country}</td><td>${c.reserves} t</td><td class="${c.change > 0 ? 'positive' : 'negative'}">${c.change > 0 ? '+' : ''}${c.change} t</td><td>${c.pct}%</td><td>${trendBadge}</td></tr>`;
                tbody.insertAdjacentHTML('beforeend', row);
            });
            
            // Graphique CB
            if (cbChart) cbChart.destroy();
            const ctx = document.getElementById('cbChart').getContext('2d');
            cbChart = new Chart(ctx, {
                type: 'bar',
                data: { labels: filtered.map(c => c.country), datasets: [{ label: 'Réserves (tonnes)', data: filtered.map(c => c.reserves), backgroundColor: '#ffd966' }] },
                options: { responsive: true, plugins: { legend: { labels: { color: '#e0e0e0' } } } }
            });
        }

        function refreshHedgeFunds() {
            const year = document.getElementById('hfYearFilter').value;
            const asset = document.getElementById('hfAssetFilter').value;
            const data = hedgeFundsData[year] || hedgeFundsData[2026];
            const filtered = data.filter(h => h.asset.toLowerCase() === asset || asset === 'oil' && (h.asset === 'WTI' || h.asset === 'Brent'));
            
            const tbody = document.querySelector('#hedgeFundsTable tbody');
            tbody.innerHTML = '';
            filtered.forEach(h => {
                const positionClass = h.position > 0 ? 'positive' : (h.position < 0 ? 'negative' : '');
                const changeClass = h.change > 0 ? 'positive' : (h.change < 0 ? 'negative' : '');
                const row = `<tr><td>${h.fund}</td><td>${h.asset}</td><td class="${positionClass}">${h.position > 0 ? '+' : ''}${h.position}k</td><td class="${changeClass}">${h.change > 0 ? '▲' : '▼'} ${Math.abs(h.change)}k</td><td><span class="badge ${h.strategy === 'Short' ? 'badge-sell' : 'badge-buy'}">${h.strategy}</span></td></tr>`;
                tbody.insertAdjacentHTML('beforeend', row);
            });
            
            if (hfChart) hfChart.destroy();
            const ctx = document.getElementById('hfChart').getContext('2d');
            hfChart = new Chart(ctx, {
                type: 'bar',
                data: { labels: filtered.map(h => h.fund), datasets: [{ label: 'Position nette (k contrats)', data: filtered.map(h => h.position), backgroundColor: filtered.map(h => h.position > 0 ? '#2ecc71' : '#e74c3c') }] },
                options: { responsive: true, plugins: { legend: { labels: { color: '#e0e0e0' } } } }
            });
        }

        function refreshSovereignFunds() {
            const tbody = document.querySelector('#sovereignFundsTable tbody');
            tbody.innerHTML = '';
            sovereignFundsData.forEach(f => {
                const row = `<tr><td>${f.name}</td><td>${f.country}</td><td>$${f.aum} Md</td><td>${f.goldExposure}%</td><td>${f.energyExposure}%</td><td>${f.lastMove}</td></tr>`;
                tbody.insertAdjacentHTML('beforeend', row);
            });
            
            const summary = document.getElementById('swfSummary');
            summary.innerHTML = `
                <div class="card"><div class="card-title">AUM Total</div><div class="card-value">$${sovereignFundsData.reduce((s,f)=>s+f.aum,0)} Md</div></div>
                <div class="card"><div class="card-title">Exposition or moyenne</div><div class="card-value">${(sovereignFundsData.reduce((s,f)=>s+f.goldExposure,0)/sovereignFundsData.length).toFixed(1)}%</div></div>
                <div class="card"><div class="card-title">Exposition énergie moyenne</div><div class="card-value">${(sovereignFundsData.reduce((s,f)=>s+f.energyExposure,0)/sovereignFundsData.length).toFixed(1)}%</div></div>
            `;
            
            if (swfChart) swfChart.destroy();
            const ctx = document.getElementById('swfChart').getContext('2d');
            swfChart = new Chart(ctx, {
                type: 'radar',
                data: { labels: sovereignFundsData.map(f => f.name), datasets: [{ label: 'Exposition Or (%)', data: sovereignFundsData.map(f => f.goldExposure), borderColor: '#ffd966', fill: true, backgroundColor: 'rgba(255, 217, 102, 0.2)' }] },
                options: { responsive: true, plugins: { legend: { labels: { color: '#e0e0e0' } } } }
            });
        }

        function refreshCommodities() {
            const tbody = document.querySelector('#commodityTable tbody');
            tbody.innerHTML = '';
            const commodities = [
                { name: 'Or (XAU/USD)', price: commoditiesData.gold.price, m1: commoditiesData.gold.change1m, m3: commoditiesData.gold.change3m, y1: commoditiesData.gold.change1y, corr: 1.0 },
                { name: 'Pétrole WTI', price: commoditiesData.wti.price, m1: commoditiesData.wti.change1m, m3: commoditiesData.wti.change3m, y1: commoditiesData.wti.change1y, corr: 0.42 },
                { name: 'Pétrole Brent', price: commoditiesData.brent.price, m1: commoditiesData.brent.change1m, m3: commoditiesData.brent.change3m, y1: commoditiesData.brent.change1y, corr: 0.43 },
                { name: 'Cuivre', price: 4.85, m1: 3.2, m3: 12.5, y1: 18.2, corr: 0.58 },
                { name: 'Gaz naturel', price: 2.95, m1: -5.5, m3: 22.0, y1: -12.0, corr: 0.15 }
            ];
            commodities.forEach(c => {
                const row = `<tr><td>${c.name}</td><td>$${c.price}</td><td class="${c.m1 > 0 ? 'positive' : 'negative'}">${c.m1 > 0 ? '▲' : '▼'} ${Math.abs(c.m1)}%</td><td class="${c.m3 > 0 ? 'positive' : 'negative'}">${c.m3 > 0 ? '▲' : '▼'} ${Math.abs(c.m3)}%</td><td class="${c.y1 > 0 ? 'positive' : 'negative'}">${c.y1 > 0 ? '▲' : '▼'} ${Math.abs(c.y1)}%</td><td>${c.corr}</td></tr>`;
                tbody.insertAdjacentHTML('beforeend', row);
            });
            
            document.getElementById('commodityStats').innerHTML = `
                <div class="card"><div class="card-title">Volatilité Or (30j)</div><div class="card-value">18.5%</div></div>
                <div class="card"><div class="card-title">Volatilité Pétrole (30j)</div><div class="card-value">32.0%</div></div>
                <div class="card"><div class="card-title">Corrélation Or/Brent</div><div class="card-value">${commodities.slice(-3)[2].corr}</div></div>
            `;
            
            if (commodityChart) commodityChart.destroy();
            const ctx = document.getElementById('commodityChart').getContext('2d');
            commodityChart = new Chart(ctx, {
                type: 'line',
                data: { labels: ['J-30', 'J-25', 'J-20', 'J-15', 'J-10', 'J-5', 'J-0'], datasets: [
                    { label: 'Or (normalisé)', data: [100, 102, 105, 108, 106, 109, 112], borderColor: '#ffd966', fill: false },
                    { label: 'Pétrole (normalisé)', data: [100, 98, 95, 97, 102, 105, 103], borderColor: '#e74c3c', fill: false }
                ] },
                options: { responsive: true, plugins: { legend: { labels: { color: '#e0e0e0' } } } }
            });
        }

        function refreshMovements() {
            const tbody = document.querySelector('#majorMovementsTable tbody');
            tbody.innerHTML = '';
            majorMovements.forEach(m => {
                const row = `<tr><td>${m.date}</td><td>${m.actor}</td><td>${m.type}</td><td>${m.asset}</td><td>${m.volume}</td><td>${m.impact}</td><td>${m.source}</td></tr>`;
                tbody.insertAdjacentHTML('beforeend', row);
            });
        }

        // ==================== ONGLETS ====================
        document.querySelectorAll('.nav-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
                document.querySelectorAll('.tab-content').forEach(t => t.classList.remove('active'));
                btn.classList.add('active');
                const tabId = btn.getAttribute('data-tab');
                document.getElementById(tabId).classList.add('active');
                
                if (tabId === 'centralbanks') refreshCentralBanks();
                if (tabId === 'hedgefunds') refreshHedgeFunds();
                if (tabId === 'sovereign') refreshSovereignFunds();
                if (tabId === 'commodities') refreshCommodities();
                if (tabId === 'movements') refreshMovements();
            });
        });

        // ==================== INITIALISATION ====================
        initGoldReservesChart();
        initHFChart();
        refreshDashboard();
        refreshCentralBanks();
        refreshHedgeFunds();
        refreshSovereignFunds();
        refreshCommodities();
        refreshMovements();

        // Auto-refresh toutes les 30 secondes (prix)
        setInterval(() => {
            refreshDashboard();
        }, 30000);
    </script>
</body>
</html>
